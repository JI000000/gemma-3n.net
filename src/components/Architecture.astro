---
import { getLangFromUrl, useTranslations } from '../i18n';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<section id="architecture" class="py-20 bg-white dark:bg-slate-900">
  <div class="max-w-6xl mx-auto px-4">
    <div class="text-center mb-16">
      <h2 class="text-4xl font-bold text-slate-900 dark:text-white mb-4">
        {t('home.architecture.title') || 'MatFormer Architecture'}
      </h2>
      <p class="text-xl text-slate-600 dark:text-gray-400 max-w-4xl mx-auto">
        {t('home.architecture.desc') || 'Gemma 3n introduces the innovative MatFormer architecture for efficient multimodal processing.'}
      </p>
    </div>
    
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
      <!-- 架构特点 -->
      <div class="space-y-8">
        <div class="bg-slate-50 dark:bg-gray-800 p-6 rounded-xl">
          <div class="w-12 h-12 bg-gradient-to-r from-blue-500 to-indigo-500 rounded-lg flex items-center justify-center text-white text-2xl mb-4">
            🏗️
          </div>
          <h3 class="text-xl font-bold text-slate-900 dark:text-white mb-3">
            {t('home.architecture.matformer') || 'MatFormer Design'}
          </h3>
          <p class="text-slate-600 dark:text-gray-400">
            {t('home.architecture.matformer_desc') || 'Novel nested Transformer architecture that adapts computation based on task complexity.'}
          </p>
        </div>
        
        <div class="bg-slate-50 dark:bg-gray-800 p-6 rounded-xl">
          <div class="w-12 h-12 bg-gradient-to-r from-green-500 to-emerald-500 rounded-lg flex items-center justify-center text-white text-2xl mb-4">
            ⚡
          </div>
          <h3 class="text-xl font-bold text-slate-900 dark:text-white mb-3">
            {t('home.architecture.efficient') || 'Efficient Processing'}
          </h3>
          <p class="text-slate-600 dark:text-gray-400">
            {t('home.architecture.efficient_desc') || 'Optimized for on-device inference with minimal memory footprint.'}
          </p>
        </div>
      </div>
      
      <!-- 架构图示 -->
      <div class="bg-gradient-to-br from-blue-50 to-indigo-100 dark:from-blue-900/20 dark:to-indigo-900/20 p-8 rounded-xl">
        <div class="text-center space-y-6">
          <div class="bg-white dark:bg-gray-800 p-4 rounded-lg shadow-sm">
            <div class="text-sm font-medium text-slate-600 dark:text-gray-400 mb-2">Input Layer</div>
            <div class="text-blue-600 dark:text-blue-400 font-semibold">Audio • Vision • Text</div>
          </div>
          
          <div class="flex justify-center">
            <div class="w-px h-8 bg-slate-300 dark:bg-gray-600"></div>
          </div>
          
          <div class="bg-white dark:bg-gray-800 p-4 rounded-lg shadow-sm">
            <div class="text-sm font-medium text-slate-600 dark:text-gray-400 mb-2">MatFormer Layers</div>
            <div class="text-purple-600 dark:text-purple-400 font-semibold">Nested Transformers</div>
          </div>
          
          <div class="flex justify-center">
            <div class="w-px h-8 bg-slate-300 dark:bg-gray-600"></div>
          </div>
          
          <div class="bg-white dark:bg-gray-800 p-4 rounded-lg shadow-sm">
            <div class="text-sm font-medium text-slate-600 dark:text-gray-400 mb-2">Output Layer</div>
            <div class="text-green-600 dark:text-green-400 font-semibold">Unified Multimodal Response</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section> 